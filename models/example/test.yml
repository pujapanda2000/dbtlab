version: 2

models:
  - name: STG_PROD
    description: "This will store product information"
    columns:
      - name: PRODUCT_ID
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
